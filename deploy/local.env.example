# Copy to deploy/local.env (gitignored) for site-specific deploy defaults.
# Sourced by deploy/deploy.sh when present.

# SSH user when host is given without user@ (default in script: pi)
RS_POOL_DEPLOY_USER=pi
